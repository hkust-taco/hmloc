:NewParser

fun f(x, y) =
  if x == 0 and
    y == 0 then "bruh"
    y == 1 then "lol"
  else "okay"
//│ f: (int, int,) -> string

class A()
class B()
//│ Defined class A
//│ Defined class B
//│ A: () -> A
//│ B: () -> B

:e
:ge
fun f(x) =
  if x == 0 and
    x is
      A() then "A"
      B() then "B"
    x == 0 then "lol"
    else "bruh"
//│ ╔══[ERROR] Missing the otherwise case of test == (x,) (0,)
//│ ║  l.20: 	  if x == 0 and
//│ ╙──      	     ^^^^^^
//│ f: anything -> error

:e
:ge
fun f(x, y) =
  if
    x == 0 and
      y == 0 then "bruh"
      else "lol"
//│ ╔══[ERROR] Missing the otherwise case of test == (x,) (0,)
//│ ║  l.35: 	    x == 0 and
//│ ╙──      	    ^^^^^^
//│ f: (anything, anything,) -> error
