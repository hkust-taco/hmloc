

// FIXME missing provenances:
//    let f x = [[x]] in
//    (f 1 + [[f true]])

let test =
  let f x = x in
  (f 1 + f true)
//│ test: int



// FIXME missing provenances:
//    let test2 = let (a, [[b]]) = test in b + 1
//    let test2 = let (a, b) = test in [[b]] + 1

let test =
  let f x = x in
  (f 1, f true)
//│ test: (int, bool,)

let test2 = let (a, b) = test in b + 1
//│ test2: int




